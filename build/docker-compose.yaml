version: "3"
services:
  postgres:
    container_name: postgres_local_testing
    image: postgres:12.3-alpine
    ports:
      - ${DB_PORT}:5432
    environment:
      - POSTGRES_DB=${DB_NAME}
      - POSTGRES_PASSWORD=${DB_PASS}
